## Interactive 3D cafe website with contour-line theme, animations, core pages, and export/download page

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
